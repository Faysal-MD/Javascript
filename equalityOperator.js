// = assignment operator
// == comparison operator (compare if values are equal)
// === strict equality operator (compare if values & datatype are equal)
// != inequality operator
// != strict inequality operator

const pi = 3.1416;

if (pi === "3.1416") {
  console.log("this's a pi");
} else {
  console.log("this isn't a pi");
}
